#Module - Python file that can be imported into other python files
import functions #imports the functions.py file we created earlier

print(functions.say_hi("Ray",42))

#Definitely don't have functions executing in the module file itself as they get executed in current script 

#Pip is a package manager for installing modules
#pip --version
#pip install python-docx
#pip uninstall python-docx